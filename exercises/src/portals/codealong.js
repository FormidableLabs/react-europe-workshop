import React, { Component } from 'react';

// export class PortalModal extends Component {

// };

// export class RegularModal extends Component {

// };

export class PortalCodealong extends Component {
  render() {
    // two identical divs with buttons w/ overflow hidden, one that has a regular dom tree modal and one that has a portal modal
    // display how dom tree modal won't break out of container properly
    // display how portal modal will break out of container
    // display event bubbling
    return (
      <div>portals!</div>
    );
  };
};

export default PortalCodealong;
